generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Artist {
  id        String   @id @default(uuid())
  name      String
  specialty String
  slots     Slot[]   // Relação: Um artista tem vários horários
}

model Slot {
  id          String       @id @default(uuid())
  artistId    String
  startTime   DateTime
  endTime     DateTime
  isBooked    Boolean      @default(false)
  
  artist      Artist       @relation(fields: [artistId], references: [id])
  appointment Appointment? // Relação: Um horário pode ter UM agendamento (opcional)
}

model Appointment {
  id          String   @id @default(uuid())
  slotId      String   @unique // O SEGREDO: Garante que 1 horário = 1 agendamento
  clientName  String
  clientEmail String
  status      String   @default("confirmed")
  
  slot        Slot     @relation(fields: [slotId], references: [id])
}

model User {
  id String @id @default(uuid())
  username String @unique
  password String
}